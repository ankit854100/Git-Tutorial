This is the first write operation on git folder.

steps to add files to git:

1.git init : For initilisation of Git.
2.git status: This command will show us which files are added to staging(intermediate area between git and original folder i.e. file are added to but not committed) are and which arn't.
3.git commit -m "commit message": To commit the files in git.					
